---     
- name: Playbook for packages.yml
  hosts: web
  vars: 
    pkgs:
      - php
      - mariadb
  tasks:
    - name: install the packages
      yum:
        name: "{{ item }}"
        state: present
      loop: "{{ pkgs }}"
