---     
- name: Playbook for packages.yml
  hosts: web
  beome: yes
  vars: 
    pkgs:
      - httpd
      - php
      - mariadb
  tasks:
    - name: install the packages
      yum:
        name: "{{ item }}"
        state: present
      loop: "{{ pkgs }}"
